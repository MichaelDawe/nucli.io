extends Control


var query = ""
var array = []


# Called when the node enters the scene tree for the first time.
func _ready() -> void:
	# fill with games from search results
	pass # Replace with function body.
	# if query in title add game index to array
	# if query in username and not already in array add game index to array
	# loop through array and add games to page same as homepage does
